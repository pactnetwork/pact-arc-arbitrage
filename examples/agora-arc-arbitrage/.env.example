# === Agora-Arc arb-agent — env template ===
# Copy to ../../.env (the pact-arc-demo root .env) so both the protocol demo
# and this example share the same wallet keys. Or copy to .env in THIS dir if
# you want the example isolated.

# --- Wallets (32-byte hex private keys, 0x-prefixed) ---
# These three are inherited from the protocol demo's .env. Set them once.
AUTHORITY_PRIVATE_KEY=0x...
SETTLER_PRIVATE_KEY=0x...
AGENT_PRIVATE_KEY=0x...

# Optional: override the burn-side wallet. Defaults to AGENT_PRIVATE_KEY.
# ARC_PRIVATE_KEY=0x...
# Optional: override the mint-side wallet. Defaults to ARC_PRIVATE_KEY (same EOA).
# BASE_PRIVATE_KEY=0x...

# --- Arc Testnet ---
ARC_RPC_URL=https://rpc.testnet.arc.network
ARC_USDC=0x3600000000000000000000000000000000000000

# --- Pact contracts on Arc (deployed 2026-05-19) ---
PACT_REGISTRY=0x056BAC33546b5b51B8CF6f332379651f715B889C
PACT_POOL=0xa6135d9C6BFA0F256B9DeBa10d76C7698329aFdE
PACT_SETTLER=0xe461CE50ef53BFC10945B101FB94b11Ec5eB591f

# --- Base Sepolia (CCTP destination) ---
BASE_RPC_URL=https://sepolia.base.org
BASE_USDC=0x036CbD53842c5426634e7929541eC2318f3dCF7e

# --- CCTP v2 (same CREATE2 address on both chains) ---
CCTP_TOKEN_MESSENGER=0x8FE6B999Dc680CcFDD5Bf7EB0974218be2542DAA
CCTP_MESSAGE_TRANSMITTER=0xE737e5cEBEEBa77EFE34D4aa090756590b1CE275
ARC_SOURCE_DOMAIN=26
BASE_SEPOLIA_DEST_DOMAIN=6

# --- Iris attestation API ---
IRIS_API_BASE=https://iris-api-sandbox.circle.com
IRIS_POLL_INTERVAL_MS=5000
IRIS_POLL_TIMEOUT_S=1800

# --- Demo parameters ---
DEMO_SLUG=arc-base-cctp-usdc
DEMO_PRINCIPAL_USDC=1000000       # 1.00 USDC bridged
DEMO_PREMIUM_USDC=50000           # 0.05 USDC premium
DEMO_TOPUP_USDC=2000000           # 2.00 USDC pool headroom
DEMO_SLA_BPS=50                   # 0.50% slippage tolerance
DEMO_SLA_LATENCY_MS=900000        # 15 min — Arc-outbound standard window

# --- Prices (mock | coingecko) ---
PRICE_SOURCE=mock
